struct MainView: View {
    var body: some View {
        VStack {
            Text("Welcome to the Annotation Workspace")
                .font(.largeTitle)
            
            Text("Start Annotating Your Dataset!")
                .font(.headline)
        }
        .padding()
    }
}
